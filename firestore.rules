rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ⚠️ DEVELOPMENT MODE: Open access for testing.
    //    Lock down with auth checks before production deployment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
